# Versioning

Profile Signal follows semantic versioning for published release packages.

- PATCH: compatible fixes and documentation/package corrections.
- MINOR: new widgets, presets, themes, or compatible runtime behavior.
- MAJOR: breaking config, marker, package layout, or behavior changes without a compatibility path.

Existing preset meanings should remain stable within a major version; new use cases should normally be introduced as new presets.
