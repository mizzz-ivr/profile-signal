# Privacy

Profile Signal v0.x is public-only by design.

The default configuration requires `privacy.public_only: true`, and private repository data is not collected and then masked later. Release installs do not require a PAT or API key for the supported public-only mode.

Generated README content, SVGs, and JSON history are committed to the user's public Profile Repository, so users should review generated diffs before enabling or changing data sources.
